# Core dependencies for Semantic RAG Pipeline
# ==========================================

# Configuration and data handling
PyYAML>=6.0
numpy>=1.21.0
pandas>=1.3.0

# Machine learning and embeddings
torch>=2.0.0
sentence-transformers>=2.2.0
scikit-learn>=1.0.0  # For similarity metrics

# System monitoring
psutil>=5.8.0

# Sparse matrix operations (for similarity storage)
scipy>=1.7.0

# Text processing and NLP
nltk>=3.7
regex>=2022.3.2
spacy>=3.4.0  # For named entity recognition
scikit-learn>=1.1.0  # For TF-IDF and additional similarity metrics

# LangChain Wikipedia loader (more reliable than wikipedia library)
langchain-community>=0.0.10
langchain-core>=0.1.0

# Text processing
beautifulsoup4>=4.11.0
requests>=2.28.0

# Visualization (for later phases)
matplotlib>=3.5.0
seaborn>=0.11.0
plotly>=5.0.0

# Logging and utilities
tqdm>=4.62.0

# Future dependencies (will be needed for later phases)
# openai>=1.0.0       # For OpenAI embeddings
# ragas>=0.1.0        # For RAG evaluation

# Note: To use spaCy NER, download the English model:
# python -m spacy download en_core_web_sm